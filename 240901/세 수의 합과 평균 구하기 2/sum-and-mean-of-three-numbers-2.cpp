#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int x,y,z;
    cin >> x >> y >> z;

    int avg, sum;
    avg = (x + y + z)/3;
    sum = (x + y + z);

    cout << "avg " << avg << endl;
    cout << "sum " << sum;

    

    return 0;
}